---
- name: Kali workstation deployment
  hosts: localhost
  connection: local
  vars_files:
    - group_vars/kali.yml
  roles:
    - apt
    - kali
    - xfce
    - docker
    - chrome 
    - autorecon
    - gowitness
